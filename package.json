{
  "name": "obsidian-calendar-pro",
  "version": "1.0.2",
  "description": "A maintained calendar for Obsidian periodic notes.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "npm run compile-test && node --test test/*.test.mjs",
    "compile-test": "tsc src/core/grid.ts src/core/periodic.ts src/licence.ts --outDir dist-test --module esnext --target es2022 --moduleResolution bundler"
  },
  "keywords": [
    "obsidian-plugin"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "latest",
    "typescript": "^5.6.0"
  }
}
